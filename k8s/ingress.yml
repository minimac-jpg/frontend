apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: harlie
spec:
  ingressClassName: traefik
  rules:
    - host: harlie.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
